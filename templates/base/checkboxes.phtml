<?php foreach ((array)$this->options->val() as $option => $label): ?>
    <div class="checkbox">
        <label>
            <input type="checkbox" name="<?= $this->name; ?>[]" id="<?= $this->id . '_' . $option; ?>" <?= $this->value->attr('on'); ?> <?= in_array($option, (array)$this->checked->val()) ? 'checked' : ''; ?>>
            <?= $label; ?>
        </label>
    </div>
<?php endforeach; ?>